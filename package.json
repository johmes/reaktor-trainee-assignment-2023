{
  "name": "reaktor-assignment-birdnest",
  "version": "0.1.0",
  "type": "module",
  "description": "Root package.json",
  "scripts": {
    "start": "yarn --cwd frontend build && node backend/server.js",
    "server": "yarn --cwd frontend build && nodemon --ext * backend/server.js",
    "postinstall": "yarn run webpack",
    "heroku-postbuild": "yarn add && cd ./frontend && yarn add && yarn build && cd ../backend && yarn add",
    "webpack": "webpack"
  },
  "author": "Johannes Mensalo",
  "license": "ISC",
  "devDependencies": {
    "connect-livereload": "^0.6.1",
    "livereload": "^0.9.3",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
  }
}